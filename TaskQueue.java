import java.io.PrintStream;import java.util.NoSuchElementException; public class TaskQueue{	private Node head = null;	private Node tail = null;	private class Node {		Task task;		Node next;		Node(Task task) {			this.task = task;			next = null;		}	}	public boolean isEmpty(){		return (head == null);	}	public void put(Task task){			Node t = tail;			tail = new Node(task);			if (isEmpty()){				head = tail;			}			else {				t.next = tail;			}	}	public void printQueue(PrintStream stream){		Node h = head;		if(h!=null) {			while (h != null) {				stream.print(h.task.time);				System.out.print(" ");				Node k = h.next;				h = k;			}		}	}	public int sumQueue(){    //epistrefei to sum ton xronon pou exei h oura		Node h = head;		int sum=0;		if(h!=null) {			while (h != null) {				sum += h.task.time;				Node k = h.next;				h = k;			}		}		return sum;	}}